# 算法题目整理

> 来源：手写题目照片。
>
> 说明：原图中部分输入格式、数据范围和个别字迹不够清晰。下面先按照能够辨认的内容整理为适合在 VS Code 中使用的 Markdown 题面；带有“待确认”标记的内容，后续拿到完整题面后再补充。

---

## 第 2 题：乘积的最后一个非零数位

### 题目描述

给定一个长度为 $n$ 的整数序列：

$$
a_1,a_2,\ldots,a_n
$$

求乘积

$$
a_1a_2\cdots a_n
$$

的最后一个非零数位。

### 输入格式

第一行输入一个整数 $n$。

第二行输入 $n$ 个整数：

$$
a_1,a_2,\ldots,a_n
$$

### 输出格式

输出乘积 $a_1a_2\cdots a_n$ 的最后一个非零数位。

### 示例说明

若：

$$
10\times 12\times 25=3000
$$

则最后一个非零数位为：

```text
3
```

### 数据范围

待确认。

---

## 第 3 题：回文子序列计数

### 题目描述

给定一个长度为 $n$、仅包含小写英文字母的字符串 $s$。

求字符串 $s$ 中有多少个子序列是回文序列。

答案对 $998244353$ 取模。

### 输入格式

第一行输入一个整数 $n$，表示字符串长度。

第二行输入一个长度为 $n$ 的字符串 $s$。

### 输出格式

输出回文子序列的数量，对 $998244353$ 取模。

### 说明

目前根据手写内容，暂按以下规则理解：

1. 统计非空子序列；
2. 子序列按照所选择的下标方案区分；
3. 只要得到的字符序列是回文串，就计入答案。

上述规则待结合完整题面确认。

### 数据范围

待确认。

---

## 第 4 题：树中所有连通点集的点权和

### 题目描述

给定一棵包含 $n$ 个结点的无根树。

第 $i$ 个结点的点权为 $a_i$。

对于树中的每一个非空连通点集，计算该点集中所有结点的点权之和。求所有连通点集的点权和之和。

答案对 $998244353$ 取模。

换言之，若 $S$ 是树中的一个非空连通点集，则它的权值为：

$$
w(S)=\sum_{i\in S}a_i
$$

要求计算：

$$
\sum_{S\text{ 为非空连通点集}}w(S)
$$

### 输入格式

第一行输入一个整数 $n$，表示结点数量。

第二行输入 $n$ 个整数：

$$
a_1,a_2,\ldots,a_n
$$

接下来 $n-1$ 行，每行输入两个整数 $u,v$，表示结点 $u$ 与结点 $v$ 之间存在一条边。

### 输出格式

输出所有非空连通点集的点权和之和，对 $998244353$ 取模。

### 数据范围

待确认。

---

## 第 7 题：所有子区间排序的最少交换次数之和

### 题目描述

给定一个长度为 $n$、仅包含 $0$ 和 $1$ 的序列：

$$
a_1,a_2,\ldots,a_n
$$

定义 $f(l,r)$ 表示：将子区间

$$
a_l,a_{l+1},\ldots,a_r
$$

变为非递减有序序列所需要的最少操作次数。

每次操作可以交换两个相邻元素。

求：

$$
\sum_{l=1}^{n}\sum_{r=l}^{n}f(l,r)
$$

答案对 $998244353$ 取模。

### 输入格式

第一行输入一个整数 $n$。

第二行输入 $n$ 个整数：

$$
a_1,a_2,\ldots,a_n
$$

其中 $a_i\in\{0,1\}$。

### 输出格式

输出所有子区间排序所需的最少相邻交换次数之和，对 $998244353$ 取模。

### 说明

一个只包含 $0$ 和 $1$ 的有序序列形如：

```text
000...0111...1
```

### 数据范围

待确认。

---

## 第 9 题：矩阵行循环移动

### 题目描述

给定一个 $n\times m$ 的矩阵，需要执行若干次行循环移动操作。

操作共有两种：

### 操作 1

```text
1 x k
```

将第 $x$ 行循环左移 $k$ 次。

每次循环左移会把该行最左端的元素移动到最右端。

例如：

```text
1 2 3 4 5
```

循环左移一次后变为：

```text
2 3 4 5 1
```

### 操作 2

```text
2 x k
```

将第 $x$ 行循环右移 $k$ 次。

每次循环右移会把该行最右端的元素移动到最左端。

例如：

```text
1 2 3 4 5
```

循环右移一次后变为：

```text
5 1 2 3 4
```

执行完所有操作后，输出矩阵的最终状态。

### 输入格式

根据手写内容推测，输入可能为：

第一行输入三个整数 $n,m,q$，分别表示矩阵行数、列数和操作次数。

接下来 $n$ 行，每行输入 $m$ 个整数，表示矩阵。

接下来 $q$ 行，每行输入一种操作：

```text
1 x k
```

或：

```text
2 x k
```

### 输出格式

输出执行所有操作后的矩阵。

### 数据范围

待确认。

---

## 第 11 题：Lowbit 博弈

### 题目描述

定义：

$$
\operatorname{lowbit}(x)=x\ \&\ (-x)
$$

其中，$\operatorname{lowbit}(x)$ 表示 $x$ 的二进制表示中最低位的 $1$ 所对应的数值。

例如：

$$
12=(1100)_2
$$

因此：

$$
\operatorname{lowbit}(12)=4
$$

现在有一个整数 $x$，两名玩家进行博弈，先手玩家先操作。

若轮到某名玩家时，当前的 $x$ 满足以下任一条件，则该玩家立即失败：

1. $x=0$；
2. $x$ 是 $2$ 的非负整数次幂。

否则，该玩家可以选择执行以下一种操作：

$$
x\leftarrow x+\operatorname{lowbit}(x)
$$

或：

$$
x\leftarrow x-\operatorname{lowbit}(x)
$$

双方都采用最优策略。判断先手玩家是否必胜。

### 输入格式

输入一个整数 $x$。

### 输出格式

如果先手玩家必胜，输出：

```text
AAA
```

否则输出：

```text
BBB
```

### 数据范围

待确认。

---

## 解题记录模板

你可以在每道题后继续补充以下内容：

```md
### 思路


### 正确性证明


### 复杂度分析

- 时间复杂度：
- 空间复杂度：

### C++ 代码

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    return 0;
}
```
```
